Discuss with Lukas:

"I see Server::run() has a loop. The spec requires ONE poll() for ALL servers. Should the event loop be in WebServ::run() instead?"
"Where should Connection objects live? In WebServ namespace or Server class?"
"What's the purpose of Listener class vs. just int listen_fd in Server?"


Agree on architecture:

Event loop location
Data ownership
Interface between parser output and runtime state


Define config format precisely:

Which NGINX directives supported?
Exact syntax for each directive?
Error handling (what's valid/invalid)?