what text encoding is, why it exists

the layer below serialisation formats.
JSON, XML, HTTP messages — all assume text encoding is solved.


the problem (mapping symbols to numbers), the layers (character set → encoding → transport)
