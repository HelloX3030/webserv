type-theoretic reification

partial vs total functions
reification: bringing failure into the type signature
Either, Maybe, dependent types for preconditions
syntactic vs semantic totality (types match ≠ logic is correct)
the specification can be wrong
pointers to: Agda, refinement types, formal verification
adversarial: proving absence of certain failure classes
