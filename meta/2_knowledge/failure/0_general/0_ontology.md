what failure IS

failure as contract deviation (requires specification to exist)
failure as entropy increase / information loss
failure exists only relative to intent
adversarial: the contract itself can be wrong, incomplete, or exploited
