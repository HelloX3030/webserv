fault → error → failure

fault (static defect, dormant)
error (invalid state, activated)
failure (observable deviation, manifested)
upstream goal: detect errors before failure, prevent faults through verification
adversarial: faults can be injected, errors can be induced
