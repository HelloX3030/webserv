the JSON Compilation Database specification. what fields exist (directory, file, command vs arguments, output). the distinction between command (shell-escaped string) and arguments (JSON array — preferred, avoids shell ambiguity). how tools discover the database (clangd: walks up directory tree from each source file; clang-tidy: --compile-commands-dir flag). multiple generation methods (bear, cmake CMAKE_EXPORT_COMPILE_COMMANDS, ninja, compiledb). scope: the format, not the tools that consume it.
