wip from ghrod

## use, incorporate:

DELETE method

test runtime interactions — e.g., can CGI coexist with uploads in the same location?
what happens when a redirect location also specifies cgi_extension?
