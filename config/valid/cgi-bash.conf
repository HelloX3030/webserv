server {
    listen 127.0.0.1:8080;
    server_name localhost;

    location /cgi-bin/ {
        root ./www;
        cgi_extension .sh;
        cgi_path /bin/bash;
        allowed_methods GET POST;
    }
}
